/**
 * @virelle/const
 *
 * Shared constants, subscription tier definitions, and feature registry
 * used across all Virelle Studios platforms.
 *
 * Import from this package instead of duplicating constants:
 *   import { SUBSCRIPTION_TIERS, CREDIT_COSTS } from "@virelle/const";
 *   import { FEATURE_REGISTRY } from "@virelle/const/feature-registry";
 */

export * from "./const";
export * from "./feature-registry";
