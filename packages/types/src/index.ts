/**
 * @virelle/types
 *
 * Shared TypeScript type definitions used across all Virelle Studios platforms:
 *   - apps/web  (Vite + React + Node/Express)
 *   - apps/mobile (Expo + React Native)
 *   - apps/desktop (Electron)
 *
 * Import from this package instead of duplicating types across repos:
 *   import type { Project, Scene, User } from "@virelle/types";
 */

export * from "./types";
export * from "./types_additions";
