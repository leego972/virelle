/**
 * @virelle/script-templates
 *
 * AI script generation prompt templates used across all Virelle Studios platforms.
 * These templates are used by the server-side AI generation pipeline.
 *
 * Import from this package:
 *   import { SCRIPT_TEMPLATES, getScriptTemplate } from "@virelle/script-templates";
 */

export * from "./scriptTemplates";
