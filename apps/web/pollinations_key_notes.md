# Pollinations API Key Creation

Dialog shows two options:
1. Publishable Key (pk_) - client-side, rate limited 1p/hour per IP
2. Secret Key (sk_) - server-side, no rate limits, recommended for production

We need the SECRET KEY for server-side video generation.
Currently "Secret Key" option is highlighted (red border).
Name: petite-primate (auto-generated)
Budget: Unlimited
Expiry: Never
Permissions: All models, Profile, Balance
